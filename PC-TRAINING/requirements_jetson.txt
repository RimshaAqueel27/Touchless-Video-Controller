# Python Requirements for Jetson Nano
# Install manually with setup_jetson.sh for best compatibility

# TensorFlow 2.3.1 (NVIDIA build)
# Install: sudo pip3 install --pre --extra-index-url https://developer.download.nvidia.com/compute/redist/jp/v44 tensorflow==2.3.1

numpy==1.18.5
h5py==2.10.0
Pillow==8.4.0

# OpenCV
# Install: sudo apt-get install python3-opencv
